// Opt in with --experimental-reduction-fusion: two observations, one traversal.
// Empty inputs produce NaN (0 / 0), as in the unfused program.
export fn rms(samples) = {
  let squares = map(samples, x => x*x);
  sqrt(sum(squares) / count(samples))
};
